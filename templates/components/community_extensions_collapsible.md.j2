---
## Community Extensions

Third-party extensions maintained by the community

{% set very_active = community_extensions | selectattr('last_activity', 'lt', 8) | list %}
{% set active = community_extensions | selectattr('last_activity', 'ge', 8) | selectattr('last_activity', 'lt', 31) | list %}
{% set stable = community_extensions | selectattr('last_activity', 'ge', 31) | selectattr('last_activity', 'lt', 91) | list %}
{% set stale = community_extensions | selectattr('last_activity', 'ge', 91) | list %}

**Total:** {{ community_extensions|length }} extensions | 🔥 Very Active (≤7d): {{ very_active|length }} | ✅ Active (≤30d): {{ active|length }} | 🟡 Stable (≤90d): {{ stable|length }} | 🟠 Stale (>90d): {{ stale|length }}

<details open markdown="1">
<summary>Click to expand/collapse community extensions table</summary>

| # | Extension | Repository | Status | Activity | Last Activity | Stars | Language | Description |
|---|-----------|------------|--------|----------|---------------|-------|----------|-------------|
{% for ext in community_extensions | sort(attribute='last_activity') -%}
{% if ext.last_activity < 8 -%}
{% set activity_badge = '🔥 Very Active' -%}
{% elif ext.last_activity < 31 -%}
{% set activity_badge = '✅ Active' -%}
{% elif ext.last_activity < 91 -%}
{% set activity_badge = '🟡 Stable' -%}
{% else -%}
{% set activity_badge = '🟠 Stale' -%}
{% endif -%}
| {{ loop.index }} | {{ ext.name | extension_link(ext.docs_url, url_validation, ext.name, ext.repository) }} | {{ ext.repository | github_link(url_validation, ext.name) }} | {{ ext.status | status_badge }} | {{ activity_badge }} | {{ ext.last_activity | datetime_with_relative(ext.last_push) }} | {{ ext.stars | number_format }} | {{ ext.language }} | {{ ext.description | truncate(80) }} |
{% endfor %}

</details>
